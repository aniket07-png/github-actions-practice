---
name: Triggers
on:
  push:
    branches:
      - main
      - staging
      
    paths:
      - 'static/**'
      - '.github/**'

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Branch Name
        run: echo "${{ github.head_ref }}"
